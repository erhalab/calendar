<resources>
    <!-- Base application theme. -->
    <style name="Theme.Kotlin3" parent="Theme.Material3.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/default_active</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <!--        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>-->
        <!-- Customize your theme here. -->
        <item name="materialDrawerStyle">@style/Widget.MaterialDrawerStyle</item>
        <item name="android:windowActivityTransitions">false</item>
        <item name="materialDrawerHeaderStyle">@style/Widget.MaterialDrawerHeaderStyle</item>
        <item name="android:navigationBarColor">@color/default_background_color
        </item> <!-- 设置导航栏背景色 -->
    </style>

    <style name="QMUI.SlideAnimation" parent="@style/QMUI.NoActionBar">
        <!--        <item name="android:windowAnimationStyle">@style/activityAnimation</item>-->
        <item name="android:navigationBarColor">@color/default_background_color
        </item> <!-- 设置导航栏背景色 -->
        <item name="android:statusBarColor">@color/default_background_color</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Launcher" parent="Theme.AppCompat.NoActionBar">
        <item name="android:navigationBarColor">@color/default_background_color
        </item> <!-- 设置导航栏背景色 -->
        <item name="android:colorBackground">@color/default_background_color</item>
        <item name="android:colorForeground">@color/default_background_color</item>
        <item name="android:startColor">@color/default_background_color</item>
    </style>

    <style name="activityAnimation" parent="@android:style/Animation">
        <item name="android:activityOpenEnterAnimation">@anim/slide_right_in</item>
        <item name="android:activityOpenExitAnimation">@anim/slide_left_out</item>
        <item name="android:activityCloseEnterAnimation">@anim/slide_left_in</item>
        <item name="android:activityCloseExitAnimation">@anim/slide_right_out</item>
    </style>

    <style name="MyEditText" parent="QMUI.SlideAnimation">
        <item name="android:colorControlNormal">@android:color/darker_gray</item>
        <item name="android:colorControlActivated">@color/default_active</item>
    </style>
</resources>